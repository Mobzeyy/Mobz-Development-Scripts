Config = {
    -- Set true if you want time to be frozen at 10:00 AM, false if it should progress normally
    freezeTimeAtTen = true,
    
    -- Set true if you want weather to cycle, false to disable
    enableWeatherCycle = true,
    
    -- Weather cycle interval in minutes (5 minutes in this example)
    weatherCycleInterval = 10
}